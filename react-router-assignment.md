# React Router Assignment (30 Questions)

## Topics Covered
- Components
- BrowserRouter
- Routes
- Route
- Link
- useNavigate
- useParams

---

## Section A: Basics of React Routing (1–6)

1. What is routing in React?
2. Why do we need React Router in a Single Page Application (SPA)?
3. What is `react-router-dom`?
4. Explain the difference between traditional page routing and React routing.
5. What happens when the URL changes in a React application?
6. Can a React application work without routing? Explain.

---

## Section B: BrowserRouter (7–10)

7. What is `BrowserRouter` in React Router?
8. Why should `BrowserRouter` wrap the entire application?
9. What error occurs if `BrowserRouter` is not used?
10. Difference between `BrowserRouter` and `HashRouter`.

---

## Section C: Routes Component (11–14)

11. What is the purpose of the `Routes` component?
12. Why do we use `Routes` instead of multiple `Route` components?
13. Can we use more than one `Routes` component in an application?
14. What happens when multiple routes match the same path?

---

## Section D: Route Component (15–19)

15. What is a `Route` component?
16. Explain the `path` and `element` props of `Route`.
17. How do you create a default route in React Router?
18. How do you implement a 404 (Page Not Found) route?
19. Can the same component be used for multiple routes? Explain with an example.

---

## Section E: Link Component (20–22)

20. What is the `Link` component used for?
21. Difference between `<a>` tag and `Link` in React Router.
22. How does `Link` improve application performance?

---

## Section F: useNavigate Hook (23–26)

23. What is the `useNavigate` hook?
24. How do you navigate programmatically after a form submission?
25. Difference between `Link` and `useNavigate`.
26. How do you navigate back to the previous page using `useNavigate`?

---

## Section G: useParams Hook (27–30)

27. What is `useParams` used for?
28. How do you create dynamic routes using `useParams`?
29. What type of value does `useParams` return?
30. Give a real-world example where `useParams` is useful.

---

## Bonus Practical Task (Optional)

- Create a React application with:
  - Home page
  - Products list page
  - Product details page using `useParams`
  - Button-based navigation using `useNavigate`
  - Navigation menu using `Link`

---
